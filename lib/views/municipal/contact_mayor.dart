import 'package:flutter/material.dart';

class ContactMayor extends StatelessWidget {
  const ContactMayor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Contact Mayor"),
        ),
        body: const Text("contact mayor"));
  }
}
