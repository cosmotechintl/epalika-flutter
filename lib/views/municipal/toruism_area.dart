import 'package:flutter/material.dart';

class TourismArea extends StatelessWidget {
  const TourismArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Tourism Area"),
        ),
        body: const Text("Tourism Screen"));
  }
}
