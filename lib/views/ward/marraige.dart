import 'package:flutter/material.dart';

class MarriageReg extends StatelessWidget {
  const MarriageReg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Marriage Registration"),
      ),
    );
  }
}
