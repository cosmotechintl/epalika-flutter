import 'package:flutter/material.dart';

class TourismAreaWard extends StatelessWidget {
  const TourismAreaWard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Tourism Area"),
      ),
    );
  }
}
