import 'package:flutter/material.dart';

class PublicForumWard extends StatelessWidget {
  const PublicForumWard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Public Forum"),
      ),
    );
  }
}
