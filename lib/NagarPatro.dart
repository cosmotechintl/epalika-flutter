import 'package:flutter/material.dart';

class NagarPatro extends StatelessWidget {
  const NagarPatro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Nagar Patro"),
        ),
        body: const Text("Calender here"));
  }
}
