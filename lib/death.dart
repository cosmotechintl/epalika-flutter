import 'package:flutter/material.dart';

class DeathReg extends StatelessWidget {
  const DeathReg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Death Registration"),
      ),
    );
  }
}
