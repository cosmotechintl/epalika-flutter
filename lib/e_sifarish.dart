import 'package:flutter/material.dart';

class ESifarish extends StatelessWidget {
  const ESifarish({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("ESifarish"),
        ),
        body: const Text("E-sifarish Screen"));
  }
}
