import 'package:flutter/material.dart';

class HelplineNumberWard extends StatelessWidget {
  const HelplineNumberWard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Helpline Number"),
      ),
    );
  }
}
